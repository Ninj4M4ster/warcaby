package serwer;

import java.net.Socket;

public class SerwerThread extends Thread {
    Socket socket

    SerwerThread(Socket socket) {
        this.socket = socket;
    }

    @Override
    public void run() {

    }
}
